# Optional: run backend locally without Docker (needs your own Postgres).
# If you test on the live stand only, you don't need this file.
#
# DATABASE_URL=postgresql+asyncpg://coffee:PASSWORD@localhost:5432/coffeeos
# SECRET_KEY=dev-secret-change-me
# CORS_ORIGINS=http://localhost:5173
